/**
 * Check if the value received by parameters is a number
 * @param {number} num Number as parameter
 * @returns {boolean} Boolean
 */
export default function isNumber(num) {
    return typeof num === "number";
  }
  